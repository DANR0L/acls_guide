import 'package:flutter/material.dart';

// Placeholder widget — used internally for action-type nodes
// The rendering is handled directly in algorithm_screen.dart
class NodeActionCard extends StatelessWidget {
  const NodeActionCard({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
